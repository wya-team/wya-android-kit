apply plugin: 'maven'

group = 'com.weiyian.android'
version = '1.0.2'

uploadArchives {
    repositories {
        mavenDeployer {
            def deployPath = file("../" + getProperty('aar.deployPath'))
            repository(url: "file://${deployPath.absolutePath}")
        }
    }
}